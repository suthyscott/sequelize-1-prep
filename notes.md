install `npm i sequelize pg pg-hstore`

Set up `db.js`

Set up skeleton in `model.js`. You should now be able to run the `model.js` file with the `node command` and see that you connected successfully

